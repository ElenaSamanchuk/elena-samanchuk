# Архив: не участвует в сборке

Файлы здесь и в корне `src/*.css` (кроме `styles/`) — наследие предыдущих итераций дизайна. Vite их не импортирует.

## CSS (корень `src/`)

- `style.css`, `apple-dark.css`, `apple-light.css`
- `portfolio-final.css`, `premium-refined.css`, `pro-finish.css`
- `performance.css`, `compat.css`, `effects-extra.css`, `tools.css`, `cursor.css`
- `styles/theme-light.css` — эксперимент светлой темы (откатан)

## TypeScript (`effects/`, не из `initSite`)

- `webglBg.ts`, `caseModal.ts`, `pipelineAutoplay.ts`
- `chatbot.ts`, `customCursor.ts`, `theme.ts`, `sound.ts`, `confetti.ts`
- `heroPromoDemo.ts`, `landingConstructor.ts`, `modernTrends.ts`
- `resumePdf.ts`, `volumeCalculator.ts`, `mechanicsVideos.ts`

## Markup (`markup/`)

- `interactiveBlocks.ts`, `mechanicsWidget.ts` — не используются в `renderSite.ts`

Перед удалением — поиск по репозиторию (`rg имя_файла`).
